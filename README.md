# JELLYWATCH

해파리 발견 신고와 해변 안전 정보를 제공하는 정적 웹앱입니다.

## 실행

`index.html`을 브라우저에서 열거나, 로컬 정적 서버로 실행하세요.

```powershell
python -m http.server 8080
```

이후 <http://localhost:8080>으로 접속합니다.

## GitHub Pages 배포

저장소의 `Settings → Pages`에서 배포 소스를 `Deploy from a branch`로 선택하고,
브랜치 `main`의 루트(`/`)를 지정하면 됩니다.
